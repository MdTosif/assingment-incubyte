{
  "installCommand": "go mod download && cd web && npm install",
  "buildCommand": "cd web && npm run build && cp -r build/* ../public",
  "outputDirectory": "public",
  "framework": null,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.go" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
